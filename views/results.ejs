<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Search Results</title>
</head>
<body class="bg-gradient-to-br from-purple-500 via-pink-400 to-blue-500 text-gray-800 min-h-screen">
    <div class="flex flex-col items-center px-6 min-h-screen">
        <!-- Results Container -->
        <div class="w-full max-w-4xl bg-white/90 shadow-lg rounded-lg mt-8 p-6 mb-10"> <!-- Added mb-10 for bottom spacing -->
            <h1 class="text-4xl font-bold text-center text-blue-600 mb-6">Search Results</h1>
            <p class="text-center mb-4 text-gray-600">
                Results for "<b><%= query %></b>" in "<b><%= category %></b>"
            </p>
            <a href="/" 
               class="inline-block bg-gradient-to-r from-blue-600 to-purple-600 text-white font-semibold py-2 px-4 rounded-lg shadow-md hover:shadow-lg hover:from-blue-700 hover:to-purple-700 transition mb-6">
                Go Back
            </a>

            <div class="space-y-4">
                <% if (blogs.length > 0) { %>
                    <% blogs.forEach(blog => { %>
                        <div class="bg-purple-100/80 p-6 rounded-lg shadow-md border border-purple-200">
                            <h2 class="text-2xl font-semibold text-blue-700 mb-2"><%= blog.title %></h2>
                            <p class="text-gray-700 mb-3"><%= blog.content %></p>
                            <span class="text-sm text-gray-500">Category: <%= blog.category %></span>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p class="text-center text-gray-600">No results found.</p>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
